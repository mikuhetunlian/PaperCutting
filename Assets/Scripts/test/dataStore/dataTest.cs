using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;






public class dataTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        //PlayerPrefs.DeleteAll();
        //playerInfo info1 = new playerInfo();
        //info1.id = 146;
        //info1.name = "yabu";
        //info1.age = 18;
        //info1.atk = 36;
        //info1.vectorPos = new List<float>();
        //info1.vectorPos.Add(150.5f);
        //info1.myDic = new Dictionary<string, int>();
        //info1.myDic.Add("liyuu", 150);
        //info1.trueName = new TrueName();
        //info1.trueName.trueName = "nako";


        //DataMgr.Instance.Save(info1, "player1");

        //playerInfo info2 = DataMgr.Instance.Load(typeof(playerInfo), "player1") as playerInfo;
        //Debug.Log(info2.name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
